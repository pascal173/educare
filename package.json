{
  "name": "educare-root",
  "version": "0.1.0",
  "private": true,
  "description": "Root folder. The real Next.js app lives in ./pharmaequip — cd into that folder to develop or deploy.",
  "scripts": {
    "dev": "echo '👉 The real app is inside pharmaequip/. Run: cd pharmaequip && npm run dev' && exit 1",
    "build": "echo '👉 cd pharmaequip && npm run build' && exit 1",
    "postinstall": "echo 'Dependencies are managed inside pharmaequip/'"
  },
  "dependencies": {},
  "devDependencies": {}
}
